# Start with the latest Ubuntu image as the base
FROM ubuntu:latest

# Update system package list and upgrade installed packages
RUN apt-get update && apt-get upgrade -y

# Set the default command to print "Hello, World!" when the container runs
CMD echo "Hello, World!"
